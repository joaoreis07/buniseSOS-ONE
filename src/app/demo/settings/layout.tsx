import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Configurações — Demonstração",
};

export default function DemoSettingsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
